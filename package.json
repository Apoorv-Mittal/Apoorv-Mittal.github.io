{
  "name": "apoorv-mittal",
  "version": "0.1.0",
  "homepage": "https://apoorv-mittal.github.io",
  "private": true,
  "dependencies": {
    "react": "^16.6.3",
    "react-dom": "^16.6.3",
    "react-router-dom": "^4.3.1",
    "react-scripts": "2.1.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "yarn build && cp -rf build/ ~/Downloads/App && git checkout master && cp -rf ~/Downloads/App/* . && git add . && git commit -m 'New Deployment' && git push origin master && git checkout react && rm -rf ~/Downloads/App"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
